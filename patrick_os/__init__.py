"""Patrick OS — reusable procedural infrastructure for repeated work.

Model-agnostic by construction: no vendor SDK is imported at module load, and
every provider is a row in `config/routes.json` rather than a branch in code.
"""

__version__ = "0.1.0"
